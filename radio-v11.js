(()=>{
'use strict';
const T=window.TM,e=T.el;
const ROOM='tommart-radio-013b88770c4c666505cac664ed9d43d8-v11';
const TOPIC='https://ntfy.sh/'+ROOM;
const SSE=TOPIC+'/sse?since=30s';
const POLL=TOPIC+'/json?poll=1&since=20s';
const BOOT=Date.now();
const delay=ms=>new Promise(r=>setTimeout(r,ms));
const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
function persistentId(){try{let id=localStorage.getItem('tommart_listener_id_v11');if(!id){id=(crypto.randomUUID?crypto.randomUUID():'u-'+Math.random().toString(36).slice(2)+Date.now().toString(36));localStorage.setItem('tommart_listener_id_v11',id)}return id}catch(_){return'u-'+Math.random().toString(36).slice(2)+Date.now().toString(36)}}
let publishQueue=Promise.resolve();
function publish(msg,{cache=true}={}){const packet={...msg,room:ROOM,ts:Date.now(),msgId:(msg.msgId||('m-'+Date.now().toString(36)+'-'+Math.random().toString(36).slice(2,7)))};const send=()=>fetch(TOPIC,{method:'POST',headers:{'Content-Type':'text/plain;charset=UTF-8',...(cache?{}:{'Cache':'no'})},body:JSON.stringify(packet),cache:'no-store',keepalive:true});publishQueue=publishQueue.then(async()=>{for(let i=0;i<3;i++){try{const r=await send();if(r.ok)return true}catch(_){}await delay(350*(i+1))}return false});return publishQueue}
function createBus(handler,statusCb){let es=null,closed=false,retryTimer=null,polling=false,lastGood=0;const seen=new Set(),seenOrder=[];const remember=id=>{if(!id||seen.has(id))return false;seen.add(id);seenOrder.push(id);if(seenOrder.length>300){const x=seenOrder.shift();seen.delete(x)}return true};const notify=()=>{try{statusCb&&statusCb(Date.now()-lastGood<9000)}catch(_){}};const parseOuter=o=>{if(!o||o.event!=='message'||!o.message)return;if(o.id&&!remember(o.id))return;try{const m=JSON.parse(o.message);if(m.room!==ROOM||!m.ts||m.ts<BOOT-120000)return;lastGood=Date.now();handler(m,o);notify()}catch(_){}};const closeSse=()=>{if(es){try{es.close()}catch(_){}es=null}};const openSse=()=>{if(closed)return;clearTimeout(retryTimer);closeSse();try{es=new EventSource(SSE);es.onopen=()=>{lastGood=Date.now();notify()};es.onmessage=ev=>{try{parseOuter(JSON.parse(ev.data))}catch(_){}};es.onerror=()=>{closeSse();retryTimer=setTimeout(openSse,1800)}}catch(_){retryTimer=setTimeout(openSse,1800)}};const poll=async()=>{if(closed||polling)return;polling=true;try{const r=await fetch(POLL,{cache:'no-store'});if(!r.ok)throw Error('poll '+r.status);const text=await r.text();for(const line of text.split('\n'))if(line.trim())try{parseOuter(JSON.parse(line))}catch(_){}lastGood=Date.now();notify()}catch(_){notify()}finally{polling=false}};openSse();poll();const pollTimer=setInterval(poll,2600);const healthTimer=setInterval(()=>{if(Date.now()-lastGood>12000)openSse();notify()},5000);const revive=()=>{if(closed)return;openSse();poll()};const vis=()=>{if(document.visibilityState==='visible')setTimeout(revive,120)};document.addEventListener('visibilitychange',vis);window.addEventListener('pageshow',revive);window.addEventListener('online',revive);window.addEventListener('focus',revive);return{restart:revive,pollNow:poll,close:()=>{closed=true;clearInterval(pollTimer);clearInterval(healthTimer);clearTimeout(retryTimer);closeSse();document.removeEventListener('visibilitychange',vis);window.removeEventListener('pageshow',revive);window.removeEventListener('online',revive);window.removeEventListener('focus',revive)}}}
T.ac=null;T.localKey='';T.playEpoch=0;
T.audioCtx=()=>{if(!T.ac){const A=window.AudioContext||window.webkitAudioContext;T.ac=new A}return T.ac};
T.unlock=async()=>{const c=T.audioCtx();if(c.state!=='running')try{await c.resume()}catch(_){}};
T.meter=(stream,cb)=>{const c=T.audioCtx(),src=c.createMediaStreamSource(stream),an=c.createAnalyser();an.fftSize=512;src.connect(an);const d=new Uint8Array(an.frequencyBinCount);let raf;const run=()=>{an.getByteFrequencyData(d);let s=0;for(const x of d)s+=x;cb(Math.min(1,(s/d.length)/100));raf=requestAnimationFrame(run)};run();return()=>{cancelAnimationFrame(raf);try{src.disconnect()}catch(_){}}};
T.srcFor=st=>st.segment==='track'?T.pathFor(T.TRACKS[st.track].file):st.segment==='radioJingle'?T.pathFor(T.RADIO_JINGLE.file):st.segment==='adJingle'?T.pathFor(T.AD_JINGLE.file):st.segment==='ad'?T.pathFor((T.ADS[st.ad]||T.ADS[0]).file):T.pathFor(T.TRACKS[0].file);
T.stateKey=st=>st?[st.segment,st.track??'',st.ad??'',st.nextTrack??'',st.sourceTrack??''].join(':'):'none';
T.stopProgram=()=>{T.playEpoch++;try{e.music.onended=null;e.music.pause();e.music.playbackRate=1}catch(_){}T.localKey=''};
T.setAudio=async(src,offset=0)=>{const abs=new URL(src,location.href).href;if(e.music.src!==abs)e.music.src=src;if(!e.music.duration||!Number.isFinite(e.music.duration))await new Promise(res=>{let done=false;const f=()=>{if(done)return;done=true;res()};e.music.addEventListener('loadedmetadata',f,{once:true});setTimeout(f,3200)});if(Number.isFinite(offset)&&offset>=0&&e.music.duration)try{e.music.currentTime=Math.min(offset,Math.max(0,e.music.duration-.08))}catch(_){}try{await e.music.play();return true}catch(_){return false}};
T.nextStateAfter=st=>{if(st.segment==='track'){const next=(st.track+1)%T.TRACKS.length,doAd=T.adAfter(st.track),doJ=T.jingleAfter(st.track),ad=T.adIndexAfter(st.track);if(doAd)return{segment:'adJingle',ad,sourceTrack:st.track,afterJingle:doJ,nextTrack:next,offset:0,dur:T.AD_JINGLE_DUR};if(doJ)return{segment:'radioJingle',sourceTrack:st.track,nextTrack:next,offset:0,dur:T.RADIO_JINGLE_DUR};return{segment:'track',track:next,sourceTrack:next,offset:0,dur:T.TRACK_DURS[next]}}if(st.segment==='adJingle')return{segment:'ad',ad:st.ad,sourceTrack:st.sourceTrack,afterJingle:!!st.afterJingle,nextTrack:st.nextTrack,offset:0,dur:T.AD_DURS[st.ad]};if(st.segment==='ad'){if(st.afterJingle)return{segment:'radioJingle',sourceTrack:st.sourceTrack,nextTrack:st.nextTrack,offset:0,dur:T.RADIO_JINGLE_DUR};return{segment:'track',track:st.nextTrack??0,sourceTrack:st.nextTrack??0,offset:0,dur:T.TRACK_DURS[st.nextTrack??0]}}if(st.segment==='radioJingle')return{segment:'track',track:st.nextTrack??0,sourceTrack:st.nextTrack??0,offset:0,dur:T.TRACK_DURS[st.nextTrack??0]};return{segment:'track',track:0,sourceTrack:0,offset:0,dur:T.TRACK_DURS[0]}};
T.playState=async(st,opt={})=>{if(!st)return false;const key=T.stateKey(st),src=T.srcFor(st),abs=new URL(src,location.href).href,same=key===T.localKey&&e.music.src===abs&&!e.music.paused;T.ui('music',st,true);if(same&&!opt.force){const wanted=Number(st.offset)||0,diff=(e.music.currentTime||0)-wanted;if(Math.abs(diff)>0.34)try{e.music.currentTime=wanted}catch(_){}else if(Math.abs(diff)>0.08)try{e.music.playbackRate=diff>0?0.975:1.025;setTimeout(()=>{try{e.music.playbackRate=1}catch(_){}},750)}catch(_){}return true}T.stopProgram();const epoch=T.playEpoch;T.localKey=key;const ok=await T.setAudio(src,Number(st.offset)||0);if(epoch!==T.playEpoch)return false;try{e.music.playbackRate=1}catch(_){}if(opt.autonext===false)e.music.onended=null;else e.music.onended=()=>{if(epoch!==T.playEpoch)return;T.playState(T.nextStateAfter(st),{force:true})};if(!ok)T.setStatus('warn','Нажми кнопку ещё раз, чтобы включить звук');else if(opt.status)T.setStatus('ok',opt.status);return ok};
T.playGlobalProgram=()=>T.playState(T.timelineState(),{force:true});
T.playOneShot=async st=>{T.stopProgram();const epoch=T.playEpoch;const ok=await T.setAudio(T.srcFor(st),Number(st.offset)||0);if(!ok)return false;await new Promise(res=>{let done=false;const finish=()=>{if(done)return;done=true;if(epoch===T.playEpoch){try{e.music.onended=null;e.music.pause()}catch(_){}}res()};e.music.onended=finish;setTimeout(finish,((st.dur||5)+1.15)*1000)});return true};
function setSyncLabel(text){const card=T.$('studioCard'),ps=T.$('peerState');if(card){const first=card.querySelector('.row span');if(first)first.textContent='TomMart Control'}if(ps)ps.textContent=text}
async function bootListener(){const id=persistentId();let wanted=false,lastRevision=0,lastSentAt=0,livePc=null,pendingIce=[],bus;function closeLive(){if(livePc){try{livePc.close()}catch(_){}livePc=null}pendingIce=[];try{e.live.pause()}catch(_){}e.live.srcObject=null}function correctedPosition(m){const base=Number(m.position)||0;const age=Number(m.sentAt)?clamp((Date.now()-Number(m.sentAt))/1000,0,1.8):0;return base+age}async function applyState(m){if(!wanted||(m.to&&m.to!==id))return;const rev=Number(m.revision)||0,sent=Number(m.sentAt)||Number(m.ts)||0;if(rev<lastRevision)return;if(rev===lastRevision&&sent&&sent<lastSentAt)return;if(rev>lastRevision)lastRevision=rev;if(sent)lastSentAt=sent;if(m.mode==='live'){T.stopProgram();T.ui('live');T.setStatus('live','Подключаю прямой эфир…');return}if(m.mode!=='music'&&m.segment){closeLive();const st={segment:m.segment,track:m.track,ad:m.ad,nextTrack:m.nextTrack,sourceTrack:m.sourceTrack,afterJingle:!!m.afterJingle,dur:Number(m.dur)||5,offset:Math.min(correctedPosition(m),Math.max(0,(Number(m.dur)||5)-.08))};T.ui(m.mode,st,true);await T.playState(st,{force:T.stateKey(st)!==T.localKey,autonext:false});T.setStatus('live',st.segment==='adJingle'?'Рекламный джингл…':st.segment==='ad'?'Реклама…':'Джингл TomMart…');return}if(m.mode==='music'){closeLive();const st={segment:m.segment||'track',track:m.track,ad:m.ad,nextTrack:m.nextTrack,sourceTrack:m.sourceTrack,afterJingle:!!m.afterJingle,offset:correctedPosition(m),dur:m.dur};await T.playState(st,{force:T.stateKey(st)!==T.localKey,status:'TomMart играет • управление онлайн'})}}async function handleOffer(m){if(!wanted||m.to!==id)return;closeLive();let servers;try{servers=await T.getIce()}catch(_){T.setStatus('warn','Для прямого эфира нужна TURN-настройка');return}const pc=new RTCPeerConnection({iceServers:servers,sdpSemantics:'unified-plan',bundlePolicy:'max-bundle'});livePc=pc;pc.ontrack=async ev=>{const s=ev.streams?.[0]||new MediaStream([ev.track]);e.live.srcObject=s;try{await e.live.play();T.setStatus('live','Ты слушаешь прямой эфир TomMart')}catch(_){e.btnText.textContent='ВКЛЮЧИТЬ ЗВУК LIVE';T.setStatus('warn','Эфир подключён — нажми кнопку')}};pc.onicecandidate=ev=>{if(ev.candidate)publish({type:'ice',from:id,to:'admin',candidate:ev.candidate.toJSON?ev.candidate.toJSON():ev.candidate},{cache:false})};pc.onconnectionstatechange=()=>{if(['failed','closed'].includes(pc.connectionState))closeLive()};try{await pc.setRemoteDescription({type:'offer',sdp:m.sdp});for(const c of pendingIce.splice(0))try{await pc.addIceCandidate(c)}catch(_){}const ans=await pc.createAnswer();await pc.setLocalDescription(ans);await publish({type:'answer',from:id,to:'admin',sdp:pc.localDescription.sdp,revision:m.revision},{cache:false})}catch(_){T.setStatus('warn','Не удалось подключить микрофон')}}async function onMessage(m){if(m.type==='state')return applyState(m);if(m.type==='offer')return handleOffer(m);if(m.type==='ice'&&m.to===id&&m.candidate){if(livePc&&livePc.remoteDescription)try{await livePc.addIceCandidate(m.candidate)}catch(_){}else pendingIce.push(m.candidate);return}if(m.type==='close-live'&&m.to===id)closeLive()}bus=createBus(onMessage,ok=>{e.right.textContent=ok?'ONLINE':'СВЯЗЬ…';setSyncLabel(ok?'ONLINE':'RECONNECT');if(ok&&wanted){publish({type:'join',id},{cache:false});publish({type:'sync-request',id},{cache:false})}});e.btn.disabled=false;e.btnText.textContent='СЛУШАТЬ TOMMART';e.right.textContent='ГОТОВО';T.setStatus('ok','Радио готово • нажми слушать');e.btn.onclick=async()=>{wanted=true;e.btn.disabled=true;await T.unlock();if(e.live.srcObject&&e.live.paused){try{await e.live.play()}catch(_){}e.btn.disabled=false;return}await T.playGlobalProgram();await publish({type:'join',id},{cache:false});await publish({type:'sync-request',id},{cache:false});bus.pollNow();setTimeout(()=>e.btn.disabled=false,420)};setInterval(()=>{if(wanted)publish({type:'heartbeat',id},{cache:false})},8500);const revive=()=>{if(!wanted)return;bus.restart();setTimeout(()=>{publish({type:'join',id},{cache:false});publish({type:'sync-request',id},{cache:false})},250)};document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible')revive()});window.addEventListener('pageshow',revive);window.addEventListener('online',revive);window.addEventListener('beforeunload',()=>{try{bus.close()}catch(_){}closeLive()})}
async function bootAdmin(){T.$('pillText').textContent='STUDIO';T.$('kicker').textContent='MOBILE BROADCAST STUDIO';T.$('title').innerHTML='ТВОЙ<br><span>ЭФИР.</span>';T.$('lead').textContent='Управление музыкой, рекламой и эфиром. Команды отправляются слушателям с автоматическим резервным каналом.';T.$('disc').classList.add('hidden');T.$('micPanel').classList.remove('hidden');T.$('studioCard').classList.remove('hidden');T.$('playlistCard').classList.remove('hidden');T.$('note').classList.remove('hidden');T.$('leftLabel').textContent='СЛУШАТЕЛЕЙ';T.$('rightLabel').textContent='ЭФИР';e.left.textContent='0';e.right.textContent='OFF';const ms=T.$('micState'),mm=T.$('micMeter'),copy=T.$('copyBtn'),rows=T.$('playlistRows'),state=T.$('playlistState'),head=T.$('playlistCard').querySelector('.playlist-title span');if(head)head.textContent='УПРАВЛЕНИЕ РАДИО';setSyncLabel('CONNECTING');let mic,stopMeter,mode='music',busy=false,wake=null,anchorTrack=0,anchorWall=Date.now(),modeStartedWall=0,special=null,revision=Date.now(),manualAd=0,afterLiveTrack=0,bus;const listeners=new Map(),pcs=new Map(),pendingIce=new Map();const bump=()=>++revision;function stateAt(){return T.timelineState(anchorTrack,anchorWall,Date.now())}function count(){const n=Date.now();for(const [id,t] of listeners)if(n-t>32000)listeners.delete(id);e.left.textContent=listeners.size;return listeners.size}function payload(to){const sentAt=Date.now();if(mode==='live')return{type:'state',to,mode:'live',revision,sentAt,position:0};if(mode!=='music'&&special)return{type:'state',to,mode,revision,sentAt,position:Math.max(0,(Date.now()-modeStartedWall)/1000),...special};const s=stateAt();return{type:'state',to,mode:'music',revision,sentAt,segment:s.segment,track:s.track,ad:s.ad,nextTrack:s.nextTrack,sourceTrack:s.sourceTrack,afterJingle:!!s.afterJingle,position:Number(s.offset)||0,dur:s.dur}}const broadcast=()=>publish(payload());const broadcastBurst=()=>{broadcast();setTimeout(broadcast,260);setTimeout(broadcast,800);if(bus)setTimeout(()=>bus.pollNow(),150)};function closePc(id){const pc=pcs.get(id);if(pc){try{pc.close()}catch(_){}pcs.delete(id)}pendingIce.delete(id)}async function offerTo(id){if(!mic||mode!=='live')return;closePc(id);let servers;try{servers=await T.getIce()}catch(_){T.setStatus('warn','TURN недоступен — музыка работает, эфир пока нет');return}const pc=new RTCPeerConnection({iceServers:servers,sdpSemantics:'unified-plan',bundlePolicy:'max-bundle'});pcs.set(id,pc);pendingIce.set(id,[]);mic.getTracks().forEach(t=>pc.addTrack(t,mic));pc.onicecandidate=ev=>{if(ev.candidate)publish({type:'ice',from:'admin',to:id,candidate:ev.candidate.toJSON?ev.candidate.toJSON():ev.candidate},{cache:false})};pc.onconnectionstatechange=()=>{if(['failed','closed'].includes(pc.connectionState))closePc(id)};try{const off=await pc.createOffer();await pc.setLocalDescription(off);await publish({type:'offer',from:'admin',to:id,sdp:pc.localDescription.sdp,revision},{cache:false})}catch(_){}}async function onMessage(m){if(m.type==='join'||m.type==='heartbeat'||m.type==='sync-request'){if(m.id){listeners.set(m.id,Date.now());count()}if(m.type!=='heartbeat'&&m.id){await publish(payload(m.id));if(mode==='live'&&mic)setTimeout(()=>offerTo(m.id),220)}return}if(m.type==='answer'&&m.to==='admin'){const pc=pcs.get(m.from);if(pc)try{await pc.setRemoteDescription({type:'answer',sdp:m.sdp});const q=pendingIce.get(m.from)||[];for(const c of q)try{await pc.addIceCandidate(c)}catch(_){}pendingIce.set(m.from,[])}catch(_){}return}if(m.type==='ice'&&m.to==='admin'&&m.candidate){const pc=pcs.get(m.from);if(pc&&pc.remoteDescription)try{await pc.addIceCandidate(m.candidate)}catch(_){}else{const q=pendingIce.get(m.from)||[];q.push(m.candidate);pendingIce.set(m.from,q)}}}async function requestWake(){if(document.visibilityState!=='visible'||!('wakeLock'in navigator)||wake)return;try{wake=await navigator.wakeLock.request('screen');wake.addEventListener('release',()=>{wake=null})}catch(_){wake=null}}bus=createBus(onMessage,ok=>{setSyncLabel(ok?'ONLINE':'RECONNECT');if(!busy)T.setStatus(ok?'ok':'warn',ok?'Студия готова • управление онлайн':'Восстанавливаю управление…');if(ok){broadcast();requestWake()}});function setSpecial(newMode,st){mode=newMode;modeStartedWall=Date.now();special={segment:st.segment,track:st.track,ad:st.ad,nextTrack:st.nextTrack,sourceTrack:st.sourceTrack,afterJingle:!!st.afterJingle,dur:st.dur};bump();draw();broadcastBurst()}function setMusic(track){anchorTrack=track;anchorWall=Date.now();mode='music';modeStartedWall=0;special=null;bump();draw();broadcastBurst()}function render(){const cp=stateAt(),info=T.titleFor(cp),next=T.nextTrackFrom(cp,anchorTrack),n=T.TRACKS[next];rows.innerHTML='';const r1=document.createElement('div');r1.className='track-row';r1.innerHTML='<div class="track-num">›</div><div class="track-info"><strong></strong><small></small></div><button class="track-play">СЛЕДУЮЩИЙ ТРЕК</button>';r1.querySelector('strong').textContent=info.title;r1.querySelector('small').textContent='ДАЛЬШЕ: '+n.title;const b1=r1.querySelector('button');b1.disabled=busy||mode!=='music';b1.onclick=skipNext;rows.appendChild(r1);const r2=document.createElement('div');r2.className='track-row';r2.innerHTML='<div class="track-num">AD</div><div class="track-info"><strong>Рекламный блок</strong><small>РЕКЛАМНЫЙ ДЖИНГЛ → РЕКЛАМА → МУЗЫКА</small></div><button class="track-play">ВКЛЮЧИТЬ РЕКЛАМУ</button>';const b2=r2.querySelector('button');b2.disabled=busy||mode!=='music';b2.onclick=playAdNow;rows.appendChild(r2)}function draw(){const cp=stateAt();e.btn.disabled=busy;e.btn.classList.toggle('on',mode==='live');e.btnText.textContent=mode==='live'?'ЗАВЕРШИТЬ ЭФИР':busy?'ПОДОЖДИ…':'ВЫЙТИ В ЭФИР';e.right.textContent=mode==='live'?'ON AIR':mode==='music'?'OFF':special?.segment==='ad'||special?.segment==='adJingle'?'AD':'JINGLE';ms.textContent=mic?'ACTIVE':'OFF';T.ui(mode==='music'?'music':mode,special||cp);render();state.textContent=busy?'ВЫПОЛНЯЮ':mode==='live'?'ЭФИР':'CONTROL'}const run=st=>T.playOneShot({...st,offset:0});async function skipNext(){if(busy||mode!=='music')return;const target=T.nextTrackFrom(stateAt(),anchorTrack);busy=true;draw();T.setStatus('warn','Переключаю трек…');setMusic(target);busy=false;draw();broadcastBurst();T.setStatus('ok','Сейчас играет: '+T.TRACKS[target].title)}async function playAdNow(){if(busy||mode!=='music')return;busy=true;const target=T.nextTrackFrom(stateAt(),anchorTrack),ad=manualAd++%T.ADS.length;draw();setSpecial('manualAd',{segment:'adJingle',ad,sourceTrack:null,afterJingle:false,nextTrack:target,dur:T.AD_JINGLE_DUR});T.setStatus('live','Рекламный джингл…');await run({segment:'adJingle',ad,nextTrack:target,dur:T.AD_JINGLE_DUR});setSpecial('manualAd',{segment:'ad',ad,sourceTrack:null,afterJingle:false,nextTrack:target,dur:T.AD_DURS[ad]});T.setStatus('live','Реклама…');await run({segment:'ad',ad,nextTrack:target,dur:T.AD_DURS[ad]});setMusic(target);busy=false;draw();broadcastBurst();T.setStatus('ok','Реклама закончилась • '+T.TRACKS[target].title)}async function startAir(){if(busy||mode==='live')return;busy=true;afterLiveTrack=T.nextTrackFrom(stateAt(),anchorTrack);draw();T.setStatus('warn','Запрашиваю микрофон…');try{await T.unlock();try{await T.getIce()}catch(x){throw Error(x.message==='TURN_KEY_MISSING'?'TURN_KEY_MISSING':'TURN_FAIL')}mic=await navigator.mediaDevices.getUserMedia({video:false,audio:{echoCancellation:true,noiseSuppression:true,autoGainControl:true,channelCount:1}});stopMeter=T.meter(mic,l=>mm.style.width=Math.max(3,Math.round(l*100))+'%');await requestWake();setSpecial('intro',{segment:'radioJingle',nextTrack:afterLiveTrack,dur:T.RADIO_JINGLE_DUR});T.setStatus('live','Джингл перед эфиром…');await run({segment:'radioJingle',nextTrack:afterLiveTrack,dur:T.RADIO_JINGLE_DUR});mode='live';modeStartedWall=Date.now();special=null;bump();busy=false;draw();broadcastBurst();for(const id of listeners.keys())offerTo(id);T.setStatus('live','ТЫ В ПРЯМОМ ЭФИРЕ')}catch(x){if(mic)mic.getTracks().forEach(t=>t.stop());mic=null;if(stopMeter)stopMeter();stopMeter=null;mm.style.width='3%';busy=false;mode='music';special=null;anchorTrack=afterLiveTrack;anchorWall=Date.now();bump();draw();broadcastBurst();T.setStatus('bad',x?.message==='TURN_KEY_MISSING'?'Для эфира нужна ссылка с TURN':x?.name==='NotAllowedError'?'Нет доступа к микрофону':'Не удалось включить прямой эфир')}}async function stopAir(){if(busy||mode!=='live')return;busy=true;for(const id of listeners.keys())publish({type:'close-live',to:id},{cache:false});for(const id of [...pcs.keys()])closePc(id);if(mic)mic.getTracks().forEach(t=>t.stop());mic=null;if(stopMeter)stopMeter();stopMeter=null;mm.style.width='3%';setSpecial('outro',{segment:'radioJingle',nextTrack:afterLiveTrack,dur:T.RADIO_JINGLE_DUR});T.setStatus('live','Джингл после эфира…');await run({segment:'radioJingle',nextTrack:afterLiveTrack,dur:T.RADIO_JINGLE_DUR});setMusic(afterLiveTrack);busy=false;draw();broadcastBurst();T.setStatus('ok','Музыка снова играет • студия готова')}e.btn.onclick=()=>mode==='live'?stopAir():startAir();copy.onclick=async()=>{let k='';try{k=localStorage.getItem('tommart_turn_key')||''}catch(_){}const u=new URL(location.origin+location.pathname);if(k)u.searchParams.set('turn',k);try{await navigator.clipboard.writeText(u.toString());copy.textContent='СКОПИРОВАНО';setTimeout(()=>copy.textContent='КОПИРОВАТЬ',1500)}catch(_){}};setInterval(()=>{count();broadcast()},2200);setInterval(()=>publish({type:'admin-ping',revision},{cache:false}),9000);const revive=()=>{if(document.visibilityState!=='visible')return;bus.restart();requestWake();setTimeout(()=>{broadcastBurst();for(const id of listeners.keys())if(mode==='live'&&mic)offerTo(id)},300)};document.addEventListener('visibilitychange',revive);window.addEventListener('pageshow',revive);window.addEventListener('online',revive);window.addEventListener('focus',revive);await requestWake();draw();broadcastBurst()}
(async()=>{try{await T.loadDurations();if(T.IS_ADMIN){document.title='TomMart — Студия';await bootAdmin()}else await bootListener()}catch(err){console.error(err);e.btn.disabled=true;e.btnText.textContent='ОШИБКА ЗАПУСКА';T.setStatus('bad','Не удалось подготовить радио');e.right.textContent='ERROR'}})();
})();